server {
  listen ${PORT};
  server_name _;

  root /usr/share/nginx/html;

  location = /health {
    return 200 'OK';
    add_header Content-Type text/plain;
  }

  location = / {
    return 302 /auth/;
  }

  location /auth/ {
    try_files $uri $uri/ /auth/index.html;
  }
}