/**
 * check database for room record.
 * return room token if found
 * create room if not found
 * get room token from y-sweet if token is not found in room
 * save room token to database
 * return room token
 */
export async function getOrCreateRoom() {
  // const existingRoom = await getRoomsByUserId(userId);
}
