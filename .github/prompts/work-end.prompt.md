---
description: "End-of-session wrap-up — capture progress and next steps"
---

1. Check `git status` and `git log` for what was done
2. Update any plan files with completed runs
3. Note any blockers or next steps
4. Summarize the session: what was accomplished, what's pending
