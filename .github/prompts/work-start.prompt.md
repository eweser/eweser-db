---
description: 'Orient at session start — read current state and priorities'
---

1. Read `ARCHITECTURE.md` for system context
2. Check `git status` and `git log --oneline -10` for recent work
3. Check for any in-progress plans in `docs/ai/plans/`
4. Summarize current state and ask what to work on
