---
'@eweser/db': patch
'@eweser/shared': patch
---

Fix published package manifests to resolve runtime and type entrypoints from built `dist` artifacts, and preserve generated declarations during the DB package build.
